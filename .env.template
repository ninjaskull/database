# Environment Variables Template
# Copy this file to .env and fill in the values for local development
# For AWS deployment, set these as environment variables in your deployment configuration

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL connection string (required)
DATABASE_URL=postgresql://username:password@host:port/database

# Individual PostgreSQL connection parameters (optional, used if DATABASE_URL not set)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_password
PGDATABASE=your_database

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Port for the application (default: 5000)
PORT=5000

# Node environment (development, production, test)
NODE_ENV=development

# =============================================================================
# AWS DEPLOYMENT
# =============================================================================
# For AWS deployment, configure these in:
# - AWS Secrets Manager (recommended for DATABASE_URL, PGPASSWORD)
# - AWS Systems Manager Parameter Store
# - ECS Task Definition environment variables
# - Elastic Beanstalk environment properties

# Example AWS Secrets Manager usage:
# Store DATABASE_URL in Secrets Manager, then reference it in your task definition:
# {
#   "name": "DATABASE_URL",
#   "valueFrom": "arn:aws:secretsmanager:region:account:secret:your-secret-name"
# }

# =============================================================================
# OPTIONAL: REPLIT-SPECIFIC (auto-populated in Replit environment)
# =============================================================================
# REPLIT_DOMAINS=
# REPLIT_DEV_DOMAIN=
# REPL_ID=
